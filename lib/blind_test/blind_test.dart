import 'package:flutter/material.dart';
import 'package:mangapad/niveau.dart';

class BlindTestHome extends StatelessWidget {
  const BlindTestHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Niveau();
  }
}
